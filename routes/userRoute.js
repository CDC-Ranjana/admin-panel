const express = require('express');
// const { addNewAdmin, loginUser } = require('../controllers/userController');
const router = express.Router();

// router.post('/add-new-user', addNewAdmin);
// router.put("/edit/:id" , editNewAdmin)
// router.delete("/delete/:id" , deleteNewAdmin)
// router.post('/login', loginUser);

module.exports = router;
